import {
  SUCCESS_REGISTER,
  ALERT_REGISTER,
  OBTAIN_USER,
  SUCCES_LOGIN,
  ALERT_LOGIN,
  SIGN_OUT,
} from "../../types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
